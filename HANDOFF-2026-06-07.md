# uFawkes.dev — Morning Handoff Prompt

# 2026-06-07

Paste this entire file at the start of your OpenCode or Claude Code session.

---

## Context

You are continuing work on uFawkes.dev, a Jekyll 4.4.1 + GitHub Pages site.
`main` is prod. Every merge auto-deploys. No npm, no Tailwind, no Astro.

Full project context is in AGENTS.md at the repo root.
Read it before touching any file.

---

## What was completed today (2026-06-06)

1. **Full UX/content audit** of the live site — found 12 issues total
   (9 on homepage, 2 on stack pages, 1 cross-cutting on learn guides)

2. **Agent system redesigned** — 7 custom agents rewritten with:

   - Correct Jekyll stack constraints (no more Astro/Tailwind references)
   - Explicit permission boundaries per agent
   - Clear deliverable formats and process steps
   - Proper skill permission gating

3. **Skills consolidated** — 29 → 12 skills. Two deleted (tailwind-tokens,
   astro-components — contradicted Jekyll hard constraints).

4. **Migration architecture decided** — agents and skills move to
   `~/.config/opencode/` (global) so they work across all uFawkes repos.

5. **Files produced** (all need to be placed in the repo):

   Agents (go to ~/.config/opencode/agents/ after consolidate-agents.sh):

   - ux.md — rewritten with Jekyll constraint awareness + live audit protocol
   - ui.md — rewritten with vanilla CSS/BEM specs (no Tailwind/Astro)
   - planning.md — new, precise AGENTS.md update protocol
   - review.md — new, strict checklist-based validator
   - build.md — new, one-file-per-task discipline enforced
   - design-system.md — new, correctly scoped to Jekyll/CSS constraints
   - infra.md — new, GitHub Pages + opencode.json maintenance

   Skills (go to ~/.config/opencode/skills/ after consolidate-agents.sh):

   - jekyll-site-conventions/SKILL.md ← most critical, ALL agents need this
   - ux-audit/SKILL.md
   - accessibility-workflow/SKILL.md
   - visual-design/SKILL.md
   - component-workflow/SKILL.md
   - responsive-layout/SKILL.md
   - design-system-workflow/SKILL.md
   - content-strategy/SKILL.md
   - cross-agent-coordination/SKILL.md
   - planning-workflow/SKILL.md
   - code-quality/SKILL.md
   - issue-format/SKILL.md

   Other files:

   - AGENTS.md — complete rewrite with Sections 1–12 + PR 4
   - consolidate-agents.sh — migration script (dry-run safe)

---

## What to do first this morning

### Step 1 — Place the new files (15 minutes)

The files above need to move from wherever they were produced into the repo.
Then run the migration:

```bash
cd ~/projects/github/paruff/uFawkes.dev

# Preview the migration first
./consolidate-agents.sh --dry-run

# Execute when ready
./consolidate-agents.sh

# Verify the report shows all ✅
```

### Step 2 — Create a feature branch

```bash
git checkout -b agent-infrastructure
git add AGENTS.md consolidate-agents.sh .opencode/opencode.json
git commit -m "chore(agents): redesign agent system and consolidate 29→12 skills"
```

### Step 3 — Run the UX agent on the live site

```bash
opencode
```

Then in the TUI:

```
@ux audit the homepage https://ufawkes.dev/ using the ux-audit skill.
Output ranked findings (P0–P3) and produce Section 8 issue rows.
```

The UX agent already has the live site audit from today. This step confirms
the new agent config works correctly before you touch any content files.

### Step 4 — Start PR 1 work (the real goal)

Once the agent system is verified, start implementing PR 1 issues.
Begin with the highest-severity items:

```
Priority order for PR 1:
#3  Remove zero-star badges (P0 — trust-damaging)
#1  Fix double H1 (P0 — accessibility + SEO)
#9  Consolidate "repo coming soon" notices (P0 — vaporware signal)
#4  Fix badge alt text (P1 — broken rendering)
#10 Add hero CTA button (P1 — conversion)
#2  Move email form below fold (P1 — conversion)
#5  Dojo callout (P2)
#12 DORA descriptions (P2)
#11 "From the blog" label (P3)
```

For each issue, use the workflow:

```
plan → @review constraints check → build (one file) → make build → @review → commit
```

---

## Current issue status

All issues are ⬜ Not started. No commits have been made to content files.
The site is unchanged from what was audited today.

Section 12 (to update after Step 3):

```
[ ] Current branch: agent-infrastructure
[ ] Last completed: none yet — infrastructure day
[ ] In progress: #D — consolidate-agents.sh migration
[ ] Blockers: none
[ ] Next task: Run @ux audit to verify new agent system, then start #3
```

---

## Hard constraints reminder (read before any file edit)

- Jekyll 4.4.1 + jekyll-feed only + vanilla CSS + vanilla JS
- `assets/css/main.css` is APPEND ONLY — never reorder
- One file per commit
- Run `make build` after every change
- Commit format: `fix(scope): description (#N)`
- Never use {% unless %}, {% case %}, or Tailwind classes

---

## Key files

| File                    | Purpose                             |
| ----------------------- | ----------------------------------- |
| AGENTS.md               | Full project context — read first   |
| index.md                | Homepage — target of PR 1           |
| assets/css/main.css     | All styles — append only            |
| \_layouts/stack.html    | Stack page layout                   |
| \_includes/header.html  | Nav — do not touch unless nav issue |
| .opencode/opencode.json | Agent permissions + MCP config      |

---

Good morning. Start with `./consolidate-agents.sh --dry-run` and verify the
output before executing. The site audit is done — today is implementation day.
